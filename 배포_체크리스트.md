# 🎯 프로젝트 관리 시스템 배포 체크리스트

## ✅ 완료된 작업
- [x] GitHub 저장소 생성 (`mogrow24/project-management-chart`)
- [x] 백엔드 배포 (`https://project-management-backend-vn80.onrender.com`)
- [x] PostgreSQL DB 생성 완료

## 🔄 진행 중인 작업

### 1단계: 프론트엔드 배포 (5분 소요)

1. **Render 대시보드 접속**: https://dashboard.render.com/

2. **"New +" 버튼 클릭** → **"Static Site"** 선택

3. **저장소 연결**:
   - Repository: `mogrow24/project-management-chart`
   - Connect 클릭

4. **설정 입력**:
   ```
   Name: project-management-frontend
   Branch: main
   Root Directory: frontend
   Build Command: npm install && npm run build
   Publish Directory: dist
   ```

5. **환경 변수 추가** (Environment Variables):
   ```
   Key: VITE_API_URL
   Value: https://project-management-backend-vn80.onrender.com
   ```

6. **"Create Static Site"** 클릭

7. 배포 완료 후 **URL 복사** (예: `https://project-management-frontend-xxxx.onrender.com`)

---

### 2단계: CORS 설정 업데이트 (1분 소요)

1. **Render 대시보드** → **project-management-backend** 선택

2. **"Environment"** 탭 클릭

3. **CORS_ORIGIN 환경 변수 찾기** 또는 추가:
   ```
   Key: CORS_ORIGIN
   Value: https://[위에서-복사한-프론트엔드-URL].onrender.com
   ```

4. **"Save Changes"** 클릭 (자동 재배포 됨, 1-2분 소요)

---

### 3단계: 데이터베이스 스키마 초기화 (5분 소요)

#### 방법 A: Render 웹 인터페이스 사용 (권장)

1. **Render 대시보드** → **PostgreSQL** 데이터베이스 선택

2. **"Query"** 또는 **"Shell"** 탭 클릭

3. 다음 SQL 실행 (순서대로):

```sql
-- 1. 스키마 생성
-- database/schema.sql 내용 복사하여 실행

-- 2. 초기 데이터
-- database/seed.sql 내용 복사하여 실행

-- 3. 스키마 업데이트
-- database/update-complete-schema.sql 내용 복사하여 실행

-- 4. 확인
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

#### 방법 B: 로컬에서 연결 (psql 필요)

```powershell
# External Database URL 사용
$DB_URL = "postgresql://project_management_jgvj_user:vklP809yzc2VHWvkRXfksbXYTvrQlvL5@dpg-d5hkg924d50c7394v05g-a.singapore-postgres.render.com/project_management_jgvj"

# 스키마 생성
psql $DB_URL -f database/schema.sql

# 초기 데이터
psql $DB_URL -f database/seed.sql

# 스키마 업데이트
psql $DB_URL -f database/update-complete-schema.sql
```

---

### 4단계: 최종 테스트 (2분 소요)

1. **프론트엔드 URL 접속**:
   - `https://[your-frontend-url].onrender.com`

2. **기능 테스트**:
   - [ ] 홈페이지 로딩 확인
   - [ ] 프로젝트 목록 조회
   - [ ] 관리자 페이지 접속 (코드: `1234`)
   - [ ] 프로젝트 생성 테스트
   - [ ] WBS 추가 테스트

3. **브라우저 콘솔 확인** (F12):
   - CORS 에러 없는지 확인
   - API 호출 성공하는지 확인

---

## 🎉 배포 완료!

모든 단계가 완료되면:
- ✅ 프론트엔드: `https://[your-url].onrender.com`
- ✅ 백엔드 API: `https://project-management-backend-vn80.onrender.com`
- ✅ 데이터베이스: PostgreSQL (Render)

---

## 📝 중요 정보

### 관리자 설정
- **관리자 코드**: `1234`
- **회사명**: 행복한 지구

### 환경 변수 요약

**백엔드:**
```
PORT=3000
DATABASE_URL=postgresql://project_management_jgvj_user:...
CORS_ORIGIN=https://[frontend-url].onrender.com
NODE_ENV=production
```

**프론트엔드:**
```
VITE_API_URL=https://project-management-backend-vn80.onrender.com
```

---

## ⚠️ 주의사항

### Render 무료 플랜 제한
- **백엔드**: 15분 비활성 후 슬립 (첫 요청 시 50초 지연)
- **데이터베이스**: 90일 후 삭제 (정기적으로 백업 권장)
- **프론트엔드**: Static Site는 항상 활성

### 성능 최적화 팁
- 프로덕션에서는 유료 플랜 권장
- 또는 Vercel(프론트엔드) + Railway(백엔드/DB) 조합

---

## 🔧 문제 해결

### CORS 에러 발생 시
1. 백엔드 CORS_ORIGIN 환경 변수 확인
2. 프론트엔드 URL이 정확히 입력되었는지 확인
3. 백엔드 재배포 (Save Changes 후 자동)

### API 연결 안됨
1. 프론트엔드 VITE_API_URL 확인
2. 백엔드 서비스 상태 확인 (Render Dashboard)
3. 브라우저 콘솔에서 에러 메시지 확인

### 데이터베이스 연결 오류
1. DATABASE_URL 환경 변수 확인
2. PostgreSQL 서비스 상태 확인
3. 백엔드 로그 확인

---

**마지막 업데이트**: 2026-01-11  
**다음 할 일**: 위의 1-4단계를 순서대로 진행하세요!





